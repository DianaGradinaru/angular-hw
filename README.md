### Acest proiect este construit folosind Angular si Typescript, se foloseste de un JSON server.

## Pentru a-l rula:

- `npm install`
- `npm run server`
- `ng serve`

Serverul ruleaza pe portul 5000.
